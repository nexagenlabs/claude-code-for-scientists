# Appendix B: what is in the companion repository

This appendix is generated by `tools/generate_appendix.py`, which reads
the repository and writes this file. It is not typed by hand, so it
cannot drift away from what the repository contains. If a build is
listed here, it is on disk. If it is on disk, it is listed here.

Regenerate it with:

```
python tools/generate_appendix.py
```

## Builds

18 builds, one per chapter that has runnable code.

| Ch | Directory | What it does | How to run it |
|---|---|---|---|
| 0 | `builds/00-reading-code` | Prints one small table five ways, so you can see what each line of pandas actually returns. | `python read_along.py` |
| 2 | `builds/02-what-an-agent-does` | Two readings of the same two files. | `python naive.py`<br>`python gated.py` |
| 4 | `builds/04-first-session` | Takes a messy plate-reader export and turns it into a tidy, versioned dataset. | `python make_fixture.py`<br>`python naive_read.py`<br>`python clean_plate.py`<br>`python tests/gate_plate.py` |
| 6 | `builds/06-prompting-like-a-reviewer` | Normalises one plate to its controls two different ways. | `python normalise_two_ways.py` |
| 7 | `builds/07-verification-gates` | Four families of verification gate over a plate pipeline, and a fault injector that seeds one problem at a time to see which family notices. | `python normalise.py`<br>`python gates.py`<br>`python inject.py` |
| 8 | `builds/08-git-and-environments` | Two things that make a result hard to reproduce. | `python bootstrap_ci.py --seed`<br>`python hidden_state.py` |
| 9 | `builds/09-literature-triage` | Screens a corpus against a versioned criteria file at two strictnesses, then measures both screens against the gold set and against each other. | `python make_corpus.py`<br>`python screen.py`<br>`python screen.py --loose`<br>`python agreement.py` |
| 10 | `builds/10-statistics-assumption-checks` | Four demonstrations of counting the wrong thing. | `python make_mtt.py`<br>`python panel.py`<br>`python pseudorep.py`<br>`python false_positive_rate.py` |
| 11 | `builds/11-survival-and-dose-response` | Fits a four-parameter logistic to a dose-response series and reports what the fit is entitled to claim. | `python make_dose.py`<br>`python fit.py`<br>`python survival.py` |
| 12 | `builds/12-publication-figures` | Regenerates a figure from its data rather than editing it by hand, then checks it against the journal's stated requirements. | `python make_figure.py`<br>`python tests/gate_figure.py`<br>`python errorbars.py`<br>`python colourcheck.py` |
| 13 | `builds/13-sequence-and-omics` | A count matrix with a known answer and two seeded design problems. | `python make_counts.py`<br>`python de.py`<br>`python filtering.py`<br>`python annotation.py`<br>`python confound.py`<br>`python read_accounting.py` |
| 14 | `builds/14-docking-prep` | Builds a receptor file carrying the defects real PDB entries carry, then inspects it before anything touches it. | `python make_pdb.py`<br>`python inspect_pdb.py`<br>`python box_and_rmsd.py`<br>`python enrichment.py` |
| 15 | `builds/15-image-and-plate-reader` | Three kinds of plate and image data, each with a known answer. | `python threshold.py`<br>`python illumination.py`<br>`python qpcr.py`<br>`python edge_effect.py` |
| 16 | `builds/16-lab-data-management` | Three mechanisms for knowing that data has not quietly changed. | `python manifest.py`<br>`python manifest.py --check`<br>`python identifiers.py`<br>`python audit_log.py` |
| 17 | `builds/17-manuscript-assembly` | Checks a manuscript against its own evidence. | `python check_numbers.py`<br>`python citations.py` |
| 18 | `builds/18-deposition` | What a deposit needs before a stranger can use it. | `python dates.py`<br>`python tests/deposit_check.py` |
| 19 | `builds/19-grant-and-thesis-drafting` | Two checks on a draft: is every claim evidenced, and is the language hedged to match the evidence behind it. | `python claim_check.py` |
| 21 | `builds/21-error-hunt` | Regenerates the counts Chapter 21 reports from `error_log.csv`, then gates them. | `python summarise_log.py     # the two tables Chapter 21 prints`<br>`python gate_log.py          # the gate` |

## Gates

A gate checks a claim and exits non-zero when the claim fails. These
are found by reading the code: a script counts as a gate when it
asserts, or exits with something other than plain zero.

`docs/gates.md` gives the command to run each one as a hook, and
explains why a hook needs `tools/as_hook.py` to block.

| Ch | Path | What it checks |
|---|---|---|
| 2 | `builds/02-what-an-agent-does/gated.py` | no description in the file |
| 4 | `builds/04-first-session/tests/gate_plate.py` | The Chapter 4 gate. |
| 7 | `builds/07-verification-gates/gates.py` | Four families of verification gate over the plate pipeline. |
| 12 | `builds/12-publication-figures/tests/gate_figure.py` | Does the figure meet the journal's stated requirements? |
| 13 | `builds/13-sequence-and-omics/read_accounting.py` | Reads in must equal reads accounted for, per sample, at every step. |
| 16 | `builds/16-lab-data-management/manifest.py` | A checksum manifest over a raw data directory, and what it notices. |
| 17 | `builds/17-manuscript-assembly/check_numbers.py` | Check every number in the prose against the results file. |
| 17 | `builds/17-manuscript-assembly/citations.py` | Resolve every DOI in the bibliography. |
| 18 | `builds/18-deposition/tests/deposit_check.py` | Would a stranger be able to use this deposit? |
| 19 | `builds/19-grant-and-thesis-drafting/claim_check.py` | Two checks on a draft: is every claim evidenced, and is it hedged to match the evidence behind it? |
| 21 | `builds/21-error-hunt/gate_log.py` | The gate for the Chapter 21 build. |
| 21 | `builds/21-error-hunt/summarise_log.py` | Summarise the defect log from Chapter 21. |
| ? | `check_setup.py` | check_setup.py, the Chapter 3 gate for Claude Code for Scientists. |
| ? | `templates/verify_plate.py` | verify_plate.py, runs after every analysis and fails loudly |

2 gate(s) are marked `?` because they sit outside `builds/` and
carry no chapter number in their path.

## Skills

No skill directories found.

`skills/README.md` lists the six flagship skills as `TODO(human)`
entries. They are written per chapter as each one is validated, and
none has been written yet. This table fills itself in as they land.

## Counts

| Thing | Number |
|---|---|
| Builds | 18 |
| Gates | 14 |
| Skills | 0 |

